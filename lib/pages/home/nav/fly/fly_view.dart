import 'package:flutter/material.dart';

class FlyView extends StatelessWidget {
  const FlyView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('fly'),
    );
  }
}
