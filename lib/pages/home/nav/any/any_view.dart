import 'package:flutter/material.dart';

class AnyView extends StatelessWidget {
  const AnyView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('any'),
    );
  }
}
